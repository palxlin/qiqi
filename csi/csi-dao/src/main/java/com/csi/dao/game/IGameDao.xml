<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csi.dao.game.IGameDao">
    <insert id="createGame" parameterType="com.csi.model.game.GameVo">
        insert into tbl_game_current
        (
        game_no,
        game_type,
        create_user,
        nums,
        remark
        )
        VALUES
        (
        #{game.gameNo, jdbcType=Integer},
        #{game.gameType, jdbcType=VARCHAR},
        #{game.createUser, jdbcType=VARCHAR},
        #{game.playerNums, jdbcType=Integer}
        #{game.remark, jdbcType=VARCHAR}
        )
    </insert>
</mapper>